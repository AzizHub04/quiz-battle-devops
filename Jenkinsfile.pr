pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Setup Node Modules') {
            steps {
                script {
                    if (isUnix()) {
                        sh 'npm ci --legacy-peer-deps || npm install --legacy-peer-deps'
                    } else {
                        bat 'npm ci --legacy-peer-deps || npm install --legacy-peer-deps'
                    }
                }
            }
        }

        stage('Build React App') {
            steps {
                script {
                    if (isUnix()) {
                        sh 'npm run build'
                    } else {
                        bat 'npm run build'
                    }
                }
            }
        }

        stage('Docker Build') {
            steps {
                script {
                    if (isUnix()) {
                        sh 'docker build -t quiz-pr .'
                    } else {
                        bat 'docker build -t quiz-pr .'
                    }
                }
            }
        }

        stage('Docker Run') {
            steps {
                script {
                    // Ensure any old container is removed before starting
                    if (isUnix()) {
                        sh 'docker rm -f quiz-pr || true'
                    } else {
                        bat 'docker rm -f quiz-pr || exit /b 0'
                    }

                    // Run the new container
                    if (isUnix()) {
                        sh 'docker run -d -p 8085:80 --name quiz-pr quiz-pr'
                    } else {
                        bat 'docker run -d -p 8085:80 --name quiz-pr quiz-pr'
                    }
                }
            }
        }

        stage('Smoke Test') {
            steps {
                script {
                    // Run CommonJS smoke test (smoke-test.cjs). We created a CJS copy of the original file.
                    if (isUnix()) {
                        sh 'node smoke-test.cjs'
                    } else {
                        bat 'node smoke-test.cjs'
                    }
                }
            }
        }

        stage('Archive Build') {
            steps {
                // Vite outputs to "dist" by default
                archiveArtifacts artifacts: 'dist/**', allowEmptyArchive: true
            }
        }
    }

    post {
        always {
            script {
                if (isUnix()) {
                    sh 'docker rm -f quiz-pr || true'
                } else {
                    bat 'docker rm -f quiz-pr || exit /b 0'
                }
            }
        }
    }
}